import { describe, it } from 'vitest';

describe('Institutional: Learning Agreement contract', () => {
  it.todo('prevents duplicate equivalence rows');
  it.todo('enforces row-level review decision guards');
  it.todo('creates safe revisions for approved rows rather than silent mutation');
  it.todo('blocks student grade edits while allowing coordinator grade actions where allowed');
});
