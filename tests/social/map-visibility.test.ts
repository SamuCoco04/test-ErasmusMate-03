import { describe, it } from 'vitest';

describe('Social: map visibility filtering contract', () => {
  it.todo('excludes hidden/removed content from map dataset');
  it.todo('returns only public-safe location context');
});
