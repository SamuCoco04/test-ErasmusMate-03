import { describe, it } from 'vitest';

describe('Social: discovery visibility contract', () => {
  it.todo('filters hidden profiles server-side');
  it.todo('enforces contactability before connection requests');
});
