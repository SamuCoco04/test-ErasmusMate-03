import { describe, it } from 'vitest';

describe('Social: connection lifecycle contract', () => {
  it.todo('allows only valid pending/accepted/rejected/cancelled transitions');
  it.todo('blocks interactions for blocked pairs');
});
