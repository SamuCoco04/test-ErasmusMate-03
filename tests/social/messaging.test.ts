import { describe, it } from 'vitest';

describe('Social: accepted-only messaging contract', () => {
  it.todo('allows messaging only between accepted connections');
  it.todo('blocks pending/rejected/blocked pairs from creating messages');
});
